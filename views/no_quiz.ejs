<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>No Quiz Available</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body, html {
            height: 100%;
            margin: 0;
            background: linear-gradient(135deg, rgba(128, 0, 128, 0.8), rgba(75, 0, 130, 0.8));
            color: white;
        }

        .header {
            background-color: white;
            height: 60px;
            display: flex;
            align-items: center;
            padding-left: 20px;
            position: fixed;
            width: 100%;
            top: 0;
            left: 0;
            z-index: 1000;
        }

        .header .logo {
            font-size: 24px;
            font-weight: bold;
            color: purple;
        }

        .container {
            margin-top: 80px; /* Space for fixed header */
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: calc(100vh - 80px); /* Full viewport height minus header */
        }

        h1 {
            color: white;
            font-weight: bold;
            margin: 20px 0; /* Margin around h1 */
        }

        p {
            color: rgba(255, 255, 255, 0.9); /* Slightly less bright for readability */
            font-size: 1.2em;
            margin: 0; /* Remove default margins */
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="logo">QuizzBee</div>
    </div>

    <div class="container">
        <h1>No Quiz Available</h1>
        <p><%= message %></p>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
